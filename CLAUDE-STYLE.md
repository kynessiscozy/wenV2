# Claude 风格改造说明

本分支（`claude-style`）把「问问大师」从**暗色玻璃拟态 / 霓虹光效**改造为
**Claude.ai 风格**：米白纸感底、赭橙单强调色、扁平卡片、宽松排版，并支持浅色 / 深色双主题。

---

## 一、设计对照

| 维度 | 改造前 | 改造后（Claude 风格） |
|---|---|---|
| 底色 | 纯黑 + 动态渐变网格 + 粒子星空 | 浅色 `#faf9f5` / 深色 `#262624`，无动态背景 |
| 强调色 | 随「用神」切换五套色相（木绿/火红/土黄/金灰/水蓝） | 统一 Claude 赭橙 `#d97757` |
| 卡片 | 半透明 + `backdrop-filter` 磨砂 + 大面积深投影 | 纯色表面 + 1px 描边 + `0 1px 2px` 极轻阴影 |
| 按钮 | 渐变玻璃胶囊 + 光晕 | 实心赭橙主按钮 / 描边幽灵次按钮 |
| 字体 | 全站无衬线，宽字距 | 标题衬线（Tiempos/宋体族）+ 正文无衬线，字距归零 |
| 光标 | 自定义跟随光球 + 光环 | 系统原生光标 |
| 底栏 | 液态玻璃 dock + 轨道动画 AI 球 | 纯色胶囊 + 柔和选中态 |
| 主题 | 仅暗色 | 浅色 / 深色 / 跟随系统，可一键切换 |

---

## 二、新增文件

| 文件 | 作用 |
|---|---|
| `src/theme-claude.css` | **设计令牌层**。定义 `--c-bg / --c-surface* / --c-text* / --c-border* / --c-accent*` 与五行色，浅深两套值 |
| `src/claude-ui.css` | **组件层**。把既有类名重塑为 Claude 外观；末尾重新声明 `--ac*` 等派生变量以覆盖 `styles.css` 的 `:root` |
| `src/ui/theme.js` | 主题切换逻辑：读写 `localStorage.tj_theme`、注入切换按钮、响应系统主题变化 |

样式加载顺序（`index.html`）：

```
theme-claude.css  →  styles.css  →  ig-dock.css  →  claude-ui.css
   令牌                原有样式        底栏样式         Claude 覆盖层
```

`index.html` `<head>` 内含一段内联脚本，在首屏渲染前写入 `data-theme`，避免明暗闪烁。

---

## 三、代码改动

### 语义化配色（codemod）
原代码在 CSS 与 JS 模板字符串里写死了上千处 `rgba(255,255,255,.xx)` / `#fff` / 深色 `rgba(0,0,0,.x)`。
`tools/` 下的脚本按 **CSS 属性上下文**（`color` / `background` / `border`）把它们批量映射到语义令牌：

| 脚本 | 作用 |
|---|---|
| `tools/claudeify.py` | 第一轮：白系颜色 → `--c-text*` / `--c-surface*` / `--c-border*` |
| `tools/claudeify2.py` | 第二轮：暗色底（低明度 rgba/hex/hsla）→ 同上 |
| `tools/strip-glass.py` | 删除 15 条 `[data-glass]`「玻璃质感强度」规则 |
| `tools/strip-shadows.py` | 中和 48 条深黑投影（在浅底上会变成脏灰光晕） |

### 手工改动
- `src/engines/shared.js` — 五行色 `WC` 改为 `var(--wx-*)`，随主题切换明度
- `src/engines/bazi.js` — 神煞标签配色改用 `color-mix()` + 语义色
- `src/ui/navigation.js` — `applyTheme()` 不再按用神改写全站色相，只写 `data-yongshen` 数据标记
- `src/render/report.js` — canvas 图表网格线/轴标签跟随明暗；剩余写死色值改为语义变量
- `src/main.js` — 图表强调色固定为赭橙；接入 `initTheme()`

---

## 四、主题切换

- 首页右上角 / 报告页顶栏各有一个 🌙☀️ 切换按钮
- 默认跟随系统；用户手动切换后固定，写入 `localStorage.tj_theme`
- 也可在控制台调用 `toggleTheme()`

---

## 五、验证

`tools/` 下的 Puppeteer 脚本用于回归检查：

```bash
npm run build
npx vite preview --port 4173 &

node tools/shot.mjs      # 首页/报告/运势/工具/AI 面板 × 明暗 → /tmp/shots
node tools/shot2.mjs     # 大师模式/工具弹层/保存弹窗 × 明暗 → /tmp/shots2
node tools/contrast.mjs  # WCAG AA 对比度审计
```

当前 `contrast.mjs` 结果：**浅色 0 处、深色 0 处**低对比文本（AA 标准，正文 4.5:1、大字 3:1）。

---

## 六、回滚

```bash
git checkout main          # 回到改造前
git diff main claude-style # 查看全部改动
```

---

## 七、细节精修（`src/claude-refine.css`）

首轮改造只统一了「配色」，第二轮针对**排版系统与交互尺寸**做了收口。

### 发现的问题（实测数据）

| 问题 | 改造前 | 改造后 |
|---|---|---|
| 字号种类 | 17 种，最小 **8.7px**（中文糊成一团） | 8 种，最小 **11px** |
| 圆角种类 | 9 种（6/8/12/15/16/17/50%/100/999px） | 4 种（12 / 16 / 999px / 50%） |
| 卡片内距 | 14~28px 混用 | 20 / 24px 两档 |
| 触控目标 | AI 面板按钮仅 **29×29** | 全部 ≥ 40×40 |

### 根因与解法

**字号失控**：旧设计用 `em` 层层相乘 —— `.qr-summary{.84em} > b{.74em}` 或
`.structure-mini-bd{.84em} > .fine-table{.7em}`，15px 基准经两三层就压到 8~9px。
解法是引入 **px 字阶**（`--fs-micro` 11px ~ `--fs-3xl` 30px），
并在中间层容器上重置 `font-size`，**阻断 em 相乘链**。

**触控目标**：`styles.css` 里有 `#aiSheet .ai-new{width:29px!important}` 这类
ID + `!important` 的窄屏规则，普通类选择器改不动，需同等特异性覆盖。

**中文排版**：原设计大量使用 `letter-spacing: 1.5px` 与 `text-transform: uppercase` ——
这两者都是为拉丁文设计的，中文加宽字距会破坏词组感，`uppercase` 更是无效。已统一归零。

### 顺带修复的 Bug
- `💾` emoji 在无 emoji 字体的环境下渲染为豆腐块 → 换成线性 SVG 图标
- 首页浮动主题按钮因通用 `display:inline-flex` 覆盖，在报告页泄漏出第二个月亮图标 → 补 `body:not(.home)` 规则

### 验证
```
node tools/contrast.mjs   # 明暗双主题均 0 处低对比
```

---

## 八、八字合盘（`src/engines/synastry.js`）

### 改造前的问题

排查时发现关系功能存在**三套并行实现**，且都不是真正的合盘：

| 位置 | 问题 |
|---|---|
| `tools/relation.js` | 收了对方生日**但从未使用**，只回显用户自己的 `d.ls` 分数 |
| `render/report.js` `calcRelation()` | 有排盘，但时辰缺失时**静默填 `06:00`**（凭空捏造时柱）；且输出「上婚/下婚」「白头偕老概率高」这类结论 |
| `main.js` v3 工具引擎 | 实际生效的那套，只有通用沟通话术，无任何命盘计算 |

更关键的是：`render/report.js` 的布局重排会把 `[data-card="relAi"]` **整个删掉**，所以那套实现用户根本看不到——这也是占位逻辑长期没被发现的原因。

### 现在的实现

新增独立引擎，三个入口统一调用：

- **真实排盘** —— 用 `mkBazi()` 为对方排盘。注意**不能调用 `mkDy()`**，它内部直接读页面上本人的 `bDate` 输入框。
- **逐柱比对** —— 天干五合/相冲、地支六合/六冲/相害/三合/相刑，按柱位加权（日柱为夫妻宫，权重 3.0）。
- **日主十神** —— 对方日主之于我是正印/七杀等，映射为「照顾支持」「推动压力」等关系角色。
- **五行互补** —— 对方命局能否补到我的用神。
- **去重** —— 月柱与日柱干支相同时会产生重复文案，展示层只保留权重最高的一条。

### 时辰缺失的处理

这是合盘最现实的约束：**多数人拿不到对方的出生时辰**。

处理方式是分级而非捏造：

| 精度 | 条件 | 说明 |
|---|---|---|
| `full` | 已知时辰 | 四柱全比对 |
| `day` | 仅知年月日 | 三柱比对，UI 明确标注 |

关键在于 **日柱不依赖时辰**（`getDayPillarIndex` 只用年月日），而日柱正是合婚中的夫妻宫——所以三柱模式下核心结论依然成立，缺失的时柱只影响子女宫与晚年节奏的判断。这一点在结果页直接向用户说明，不含糊过去。

### 文案取向

移除了「上婚/下婚」「白头偕老概率高」等结论式判断，改为描述差异与给出沟通方式，并统一加注：

> 合盘用于理解彼此差异、找到沟通方式，不预测关系结局，也不构成是否开始或结束一段关系的建议。

### 测试

```
node tools/test-synastry.mjs   # 15 项，含日柱锚点校验与 600 组随机样本边界检查
```

日柱正确性以公认锚点交叉验证：`2000-01-01 = 戊午`、`2024-01-01 = 甲子`。

---

## 九、合盘对象档案与分享

### 一处更正

上一轮我判断「命盘没有持久化」，**这个结论是错的**。当时只 grep 了 `localStorage`，
漏掉了 `main.js` 中已有的一整套 IndexedDB 实现（`initDB / dbPut / dbGetAll / dbDel`）。
实测确认：保存 → 刷新 → 重新载入全部正常，`loadProfile()` 也会自动触发 `calc()` 进入报告页。

因此本轮不重做地基，只补真正缺失的两块：**合盘对象无法保存**、**结果无法分享**。

### 新增

**`src/state/partners.js`** —— 合盘对象存储
- 用 `localStorage` 而非 IndexedDB：这是一份很轻的数据（姓名 + 生日 + 可选时辰），
  也避免与 profiles 的 schema 纠缠
- 同名 + 同生日视为同一人，更新而不是产生重复条目
- 上限 20 条，按最近使用排序

**`src/tools/synastry-share.js`** —— 分享与对象选择器
- 三个入口共用，避免再次出现「多套实现各自漂移」
- 表单顶部展示「最近合盘对象」，点击回填姓名/生日/时辰/关系类型
- 结果页底部提供「分享结果」「保存这个人」

### 分享的降级链

原本只做到「原生分享 → 剪贴板」，但测试中发现剪贴板 API 在权限受限时会直接抛错，
用户只会收到一句「当前环境不支持分享」，**文本就此丢失**。改为四级降级：

```
navigator.share  →  clipboard.writeText  →  execCommand('copy')  →  可选中文本弹层
```

其中 `AbortError`（用户主动取消原生分享）不再继续降级。
实测在剪贴板被拒的无头环境下，`execCommand` 成功兜住。

### 测试

```
node tools/test-partners.mjs   # 7 个场景：保存 / 分享 / 回填 / 刷新留存 / 删除
node tools/test-persist.mjs    # 命盘档案持久化回归
```

注：`loadProfile()` 会触发约 2 秒的推演动画，断言需等待其结束——
最初的测试因等待不足而误报「未跳转报告页」。

---

## 十、每日触点（`src/engines/liuri.js`）

### 修掉的一处死代码

旧「流日日签」里有这样一行：

```js
const relation = dayZhi === ((typeof __TJX_V5!=='undefined' && '') || '') ? '' : '流日地支「'+dayZhi+'」提示：安排留白…';
```

条件恒为 `false`，所以**地支比对从未真正发生**，每天都输出同一句通用提示。
这正是 Co-Star 被批评的模式：片段按天轮换，彼此不感知，也不与用户命盘产生关系。

### 新引擎的做法

先把「当日干支 × 本人四柱」的全部互动算出来（六合／相冲／相害／天干五合，
按柱位加权：日柱 3.0 > 月柱 1.8 > 时柱 1.4 > 年柱 1.0），
叠加「今日五行 与 用神」的生克关系，汇总成一个 `energy` 值与单一 `tone`，
**再由 tone 统一驱动全部文案**。

这样能避免竞品那种「周一让你谨慎、周二让你大胆」的自相矛盾——
同一天内所有建议来自同一个基调。

输出示例（真实运行结果）：

> 流日「申」与你时柱「巳」六合——晚上的时间比平时更容易配合。
> 有需要沟通或请求协助的事，今天开口成本较低。

这句话依赖用户的时柱，**无法套用到别人身上**。

### 刻意不做的事

- 不输出吉凶断言（测试用 10 个词做黑名单校验：大凶／必有／破财／血光／小人…）
- 不制造紧迫感来促使打开
- 同一天内容固定（`getDayPillarIndex` 是纯函数，同日必然同结果），
  不做「刷新就换一句」的伪随机

基调分布实测（400 天）：`steady 144 / flow 128 / friction 89 / rest 39`——
偏正向，不靠制造焦虑维持打开率。

### 首页「今日一句」

有档案时在**表单之前**展示当日内容，回访用户无需重新推演即可看到。
无档案时不显示，不打扰新用户。

### 顺带修掉的布局 Bug

`.p1-scroll` 用 `justify-content: center` 做垂直居中。内容超过一屏时，
flex 会把溢出部分推到 `scrollTop = 0` **之上**，导致「今日一句」滚动到顶也看不见
（实测 `scrollTop: 0` 但 `cardTop: -163`）。
改用 `margin: auto` 实现居中——内容不足时依然居中，超高时正常从顶部开始。

### 测试

```
node tools/test-liuri.mjs   # 12 项：干支锚点 / 同日稳定 / 因人而异 / 400 天自洽 / 无吉凶用词
node tools/test-daily.mjs   # 6 个 E2E 场景
```

---

## 十一、可读性改造（P0 + P1）

### 诊断依据

实测新手 / 大师两档的术语密度：

| 模式 | 正文 | 术语出现 | 密度 |
|---|---|---|---|
| 新手 | 341 字 | 2 次 | **0.6%** |
| 大师 | 493 字 | 45 次 | **9.1%** |

新手模式本身克制得不错（1 屏读完），真正的问题是**两档之间是悬崖不是台阶**：
点「查看完整专业依据」后密度跳 15 倍，且「格局／命局／喜神」等 13 个高频词
**完全没有解释入口**。

### P0：补齐术语，并让用户知道「能点」

1. **词库 34 → 55 条**，补全报告中出现却无解释的 13 个词，
   并把十神格局补成完整一套（食神格／伤官格／正官格／七杀格／正财格／
   偏财格／印绶格／比肩格／劫财格／建禄格），因为格局是新手最先撞上的词。

2. **可供性**：原设计只有 1px 虚线 + `cursor:help`——移动端没有 hover，
   用户根本不知道能点。现改为强调色 + 上标问号 + 点按反馈。

3. **一次性引导**：首次进报告显示「看到不懂的词？带虚线的术语都可以点开看白话解释」，
   关闭或点过任意术语后不再出现（`localStorage: tj_gloss_hint_seen`）。

实现上踩到一个坑：`calc()` 调用的是 **import 进来的** `showPage2`，
包装 `window.showPage2` 完全不生效；改为监听 `body.report-active` 类变化。

### P1：首屏从「数据」改为「数据 + 含义」

改造前四个格子是：`生肖 马 / 日主 辛 / 有利方向 水元素 / 当前大运 —`，
对小白而言是四个孤立符号。现在每格都带一句话：

| 改造前 | 改造后 |
|---|---|
| 日主　辛 | **我是什么样的人**　辛（金）<br>像珠玉，精致、对细节要求高 |
| 有利方向　水元素 | **对我有利的方向**　水元素<br>保持灵活，多观察再动 |
| 当前大运　— | **当前人生阶段**　壬辰<br>27–36岁这十年 |

### 顺带修掉的一个既有 Bug

「当前大运」格**恒为空**。原因是代码读的是 `ctx.dw`，
而 `dw` 在 context 里是**日主五行**（如「金」），当前大运其实在 `ctx.cDy`。
这个 bug 在改造前就存在，因为该格一直显示「—」而没被注意到。

### 测试

```
node tools/test-gloss.mjs        # 引导出现/消失/不重复 + 新词条可解释
node tools/test-firstscreen.mjs  # 首屏四格含义
```

---

## 十二、把「不会问」变成「点一下就问对」（P2）

### 起因：一次真实的答非所问

调研指出新手最大的障碍是「看不懂，又不知道怎么问」。
实测验证了这一点——向 AI 提问「食神格是什么意思？」，返回的却是：

> **我最近为什么压力大？** 近期官杀气重，压力指数偏高…

追查后发现三个叠加的 Bug：

1. **术语分支被长度门槛挡住**：`smartAnswer` 里写的是 `if(term && q.length<=10)`，
   而这句话有 11 个字，直接落到模糊 FAQ 匹配上。
2. **两份术语表已经漂移**：报告用的 `GLOSSARY`（55 条）与 AI 用的 `KB.terms`（23 条）
   各自维护，`食神格` 只存在于前者。
3. **FAQ 兜底门槛过低**：错配项以 8.4 分通过了 `sc>=8` 的判定。

### 修复

- `findTerm` 改为**取最长匹配**（否则「食神格」会被「食神」抢先命中），
  并把 `GLOSSARY` 注入检索池，两份词表统一。
- 术语分支门槛从「字数 ≤10」改为**看提问意图**（是什么／什么意思／怎么理解…）。
- FAQ 兜底门槛 `8 → 12`：宁可交给 AI，也不要给出无关答案。

### 新增：「这段是什么意思」

`src/ai/explain.js` 会把用户点击的那张卡片，自动组装成一个结构化提问：

> 「你有推进事情的魄力，也容易对自己要求很高」这段是什么意思？
> （原文：你通常愿意先站出来解决问题，适合负责需要推动、协调或决断的事…）

用户不需要自己想怎么问——这正是竞品指出的痛点。

实现中的坑：`querySelector('.bb-text, .at, p')` 是按**文档顺序**返回的，
不按选择器优先级，因此会先命中评分行等无关文本。改为逐个选择器依次尝试。

### 视觉克制

问号提示只加在**每张卡片第一个可见术语**上。
大师模式一屏有 58 个术语，全加问号会变成噪音；现在是 5 个。
判断可见性是必须的——新手/大师会隐藏大量卡片，
标到隐藏元素上会导致用户实际看到的那个反而没提示。

### 顺带修复

P1 改写首屏时移除了原有的可点术语，导致**新手模式一个可点术语都没有**
（58 个术语全在 `display:none` 的 `.qr-card` 里）。
现已把「日主／用神／大运」作为标签的一部分恢复为可点。

### 测试

```
node tools/test-explain.mjs   # 入口注入 / 自动提问 / 术语不再答非所问
```

---

## 十三、UI 缺陷修复（360 / 430 / 768 三档实测）

### 方法

新增 `tools/audit-layout2.mjs`，在三种视口 × 七个界面下自动检测：
横向溢出、文字裁切、触控目标过小、卡片重叠。
排除了两类误报：滑出视口的 `.page`（页面切换动画所需）与 sr-only 元素。

### 修复项

| # | 问题 | 实测数据 | 处理 |
|---|---|---|---|
| 1 | **工具中心 360px 塌成单列** | `grid-template-columns: 328px`，116px 高的卡只放两行短字，首屏仅 3 个工具 | 固定两列，卡高 116→86px，首屏 8 个 |
| 2 | **分类筛选条末项被切断** | `scrollWidth 374 > clientWidth 332`，无任何可滑动提示 | 右侧渐隐遮罩 + 滚动吸附 |
| 3 | **弹层关闭按钮压住简介卡** | close `303,93→345,135` 与 intro `23,109→337,245` 实测重叠 | 简介右侧留 52px；并移除背景图（导致文字对比度不稳） |
| 4 | **模式切换按钮高 30px** | 低于 WCAG 2.5.8 / iOS HIG | 统一 36px |
| 5 | **导出/导入按钮 48×29** | 内联样式写死，尺寸不足 | 抽为 `.pf-io-btn`，36px |
| 6 | **工具卡内部空隙过大** | 隐藏的 `.tool-icon` 仍带 `margin-bottom:12px` | 归零 |

### 一个值得记的坑

`.mode-top-switch` 只加 `min-height` 无效——`styles.css` 里有
`max-height:30px !important` 钳住高度，必须 `height/min-height/max-height`
三者同时覆盖。这类「同一属性被多层 `!important` 反复覆写」在本项目里很常见，
改样式前建议先用 CDP `getMatchedStylesForNode` 查清胜出规则。

### 回归

单测 15/15、E2E 6 套件全过、明暗对比度各 0 处不达标。
